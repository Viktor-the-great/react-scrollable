import {
  Meta,
  Title,
  Description,
  Primary,
  ArgTypes,
  Stories,
} from '@storybook/addon-docs/blocks';

import * as ScrollableStories from './scrollable.stories';

<Meta of={ScrollableStories} />

<Title />

<Description />

<Primary />

## Supported properties:

Accepts all `HTML` element properties for the inner `overflow` element, plus additional properties listed in the table below:

Компонент принимает все свойства `html` элемента, пробрасываемые в прокручиваемую область с CSS правилом `overflow`, и дополнительные, описанные ниже в таблице:

<ArgTypes />

## Examples

<Stories title=""/>
