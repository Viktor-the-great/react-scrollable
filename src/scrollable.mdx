import {
  Meta,
  Title,
  Description,
  Primary,
  ArgTypes,
  Stories,
  Story,
  Canvas,
} from '@storybook/addon-docs/blocks';

import * as ScrollableStories from './scrollable.stories';
import * as StyledScrollableStories from './styled-scrollable.stories';

<Meta title="Docs" />

<Title of={ScrollableStories} />

<Description of={ScrollableStories} />

<Primary of={ScrollableStories} />

## Supported properties:

Accepts all `HTML` element properties for the inner `overflow` element, plus additional properties listed in the table below:

<ArgTypes of={ScrollableStories} />

## Examples

<Canvas of={ScrollableStories.HorizontallyAndVerticallyScrollable} />
<Canvas of={ScrollableStories.HorizontallyScrollable} />
<Canvas of={ScrollableStories.VerticallyScrollable} />
<Canvas of={ScrollableStories.NotScrollable} />
